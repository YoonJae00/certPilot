# 골든셋 평가 리포트 — 2026-08-26

`make eval` 이 만든 자동 리포트다. PRD §8 "평가(골든셋)" 의 지표를 그대로 계산한다.

## 1. 실행 정보

| 항목 | 값 |
| --- | --- |
| 프로젝트 | 데모핀테크 ISMS-P 간편인증 |
| 모의심사 id | `aaad47be-3d3d-4f0c-b061-28dada26a852` |
| 판정 모델 | fake-deterministic-auditor |
| 모의심사 완료 시각 | 2026-08-25 17:52 UTC |
| 판정 수 | 101개 |
| 골든셋 | data/eval/golden.yaml (버전 0.1, 20개 케이스) |

## 2. 지표 요약

| 지표 | 값 | 비고 |
| --- | --- | --- |
| 전체 일치율 | 55.0% | 골든셋 20개 중 11개 일치 |
| 미충족 정밀도 | 0.857 | 미충족이라 한 7개 중 6개 적중 |
| 미충족 재현율 | 0.750 | 미충족이어야 할 8개 중 6개 검출 |
| 미충족 F1 | 0.800 | 정밀도·재현율 조화평균 |
| 판단불가 비율(전체) | 1.0% | 1/101 |
| 판단불가 비율(골든셋) | 0.0% | 골든셋 케이스 안에서만 |
| 근거 참조 유효율 | 100.0% | 목표 100% · 인용 309건 중 309건 실존 |
| 항목당 평균 비용 | $0.000000 | 실행 비용 $0 ÷ 101개 |

## 3. 판정 분포

| 판정 | 건수 | 비율 |
| --- | --- | --- |
| 충족(met) | 47 | 46.5% |
| 부분충족(partial) | 27 | 26.7% |
| 미충족(unmet) | 26 | 25.7% |
| 판단불가(unknown) | 1 | 1.0% |

## 4. 골든셋 대조

| 항목 | 기대 | 실제 | 판정 주체 | 일치 | 기대값 근거(요약) |
| --- | --- | --- | --- | --- | --- |
| 1.1.2 | 충족(met) | 충족(met) | LLM | O | CISO 지정 공문(02)에 지정 근거·지정 내용(CISO 겸 CPO)·역할과 책임·시행일이 모두 있고 전 임직원 수신으로 공지됐다. 정책서(0… |
| 1.2.3 | 미충족(unmet) | 충족(met) | LLM | X | 위험평가 보고서(04)는 2023년도 것 하나뿐이고, 본문에 "차기 위험평가는 2024년 3분기 예정이었으나 수행되지 않았다"고 적혀 있다. 연… |
| 2.2.4 | 부분충족(partial) | 부분충족(partial) | LLM | O | 보안 교육 이수 기록(09)에 2024년 전사 교육 1회와 개인정보취급자 교육 2회 실시 내역이 있어 계획·수행은 확인된다. 다만 개인정보취급자… |
| 2.4.2 | 판단불가(unknown) | 충족(met) | LLM | X | 물리 보안을 다루는 문서가 시드에 없다. 자산 목록(03)에 "본사 사무실" 위치 표기만 있을 뿐 보호구역 출입 통제 절차·출입 기록이 제출되지… |
| 2.5.1 | 미충족(unmet) | 미충족(unmet) | 규칙 | O | aws.iam.key_age 점검이 fail 이다(활성 액세스 키 9개 중 90일 초과 2개: demo-dev-01 142일, demo-ops-… |
| 2.5.3 | 미충족(unmet) | 미충족(unmet) | 규칙 | O | aws.iam.user_mfa 점검이 fail 이다(콘솔 사용자 7명 중 MFA 설정 3명, 미설정 4명). 루트 MFA(aws.iam.root… |
| 2.5.4 | 충족(met) | 미충족(unmet) | LLM | X | aws.iam.password_policy 가 pass 다(정책 존재, 최소 길이 10자, 복잡도 요구, 만료 주기 90일). 정보보호 정책서(… |
| 2.5.5 | 부분충족(partial) | 충족(met) | LLM | X | aws.iam.admin_users 는 warn 이다(AdministratorAccess 직접 부여 2명, demo-admin-01·demo-o… |
| 2.5.6 | 미충족(unmet) | 미충족(unmet) | 규칙 | O | aws.iam.key_age fail 에 더해, 접근권한 검토 이력(07)에서 2024년 3·4분기 검토가 "수행되지 않음"으로 비어 있고 20… |
| 2.6.1 | 부분충족(partial) | 부분충족(partial) | LLM | O | aws.ec2.open_sg 는 pass 다(0.0.0.0/0 → 22·3389·3306·5432 인바운드 0건). 다만 정보보호 정책서(01)… |
| 2.6.2 | 미충족(unmet) | 미충족(unmet) | 규칙 | O | 2.10.2 와 같은 aws.s3.public_block fail 이 매핑된 항목이다(data/rules/aws_rules.yaml). 정보시스… |
| 2.6.5 | 판단불가(unknown) | 부분충족(partial) | LLM | X | 무선 네트워크를 언급한 문서가 없고 매핑된 클라우드 점검도 없다. AWS 점검 10개는 IAM·CloudTrail·S3·RDS·EC2 보안그룹만… |
| 2.7.1 | 충족(met) | 충족(met) | LLM | O | aws.s3.encryption(버킷 6/6 암호화)과 aws.rds.encryption(인스턴스 2/2 저장 암호화)이 모두 pass 다. 정… |
| 2.9.3 | 미충족(unmet) | 충족(met) | LLM | X | 백업 정책(10)이 제7조에서 반기 1회 이상 복구 테스트를 요구하지만 수행 기록이 전혀 없다(부록 D 10번 문서의 의도). aws.rds.e… |
| 2.9.4 | 충족(met) | 부분충족(partial) | LLM | X | aws.cloudtrail.enabled 가 pass 다(전 리전 트레일 1개 + 로그 파일 무결성 검증). 정보보호 정책서(01) 제15조도 … |
| 2.10.2 | 미충족(unmet) | 미충족(unmet) | 규칙 | O | 최신 스냅샷의 aws.s3.public_block 점검이 fail 이다(버킷 6개 중 1개 demo-fintech-public-assets 의 … |
| 2.11.4 | 미충족(unmet) | 미충족(unmet) | LLM | O | 침해사고 대응 절차서(12) 제5조가 연 1회 이상 모의훈련을 정하고 있으나, 2023년 도상훈련 1회 뒤 2024년 훈련은 계획만 있고 수행되… |
| 2.11.5 | 충족(met) | 부분충족(partial) | LLM | X | 침해사고 대응 절차서(12)에 사고 등급·대응 조직·탐지/초동/분석/복구/신고·통지/ 사후조치 절차와 보고 시한이 정의돼 있다. 훈련(2.11.… |
| 3.1.6 | 판단불가(unknown) | 부분충족(partial) | LLM | X | 영상정보처리기기(CCTV) 설치·운영을 다루는 문서가 시드에 없다. 개인정보 처리방침 (05)에도 영상정보 수집 항목이 없어 해당 여부조차 확인… |
| 3.5.1 | 충족(met) | 충족(met) | LLM | O | 개인정보 처리방침(05)에 수집 항목·처리 목적·보유 기간·제3자 제공·위탁·정보주체 권리와 행사 방법이 모두 기재돼 있고 시행일·개정일이 명시… |

## 5. 근거 참조 유효성

- 근거를 인용한 판정: 100개 / 전체 101개
- 인용된 참조: 309건 (유효 309건, 무효 0건)
- 무효 참조를 포함한 판정: 0개
- 무효 참조 없음. 존재하지 않는 근거를 인용한 판정은 파이프라인에서 폐기되므로(PRD §6 환각 방지) 이 값이 100% 가 아니면 회귀다.

## 한계 (읽는 사람이 반드시 알아야 하는 것)

- 골든셋 기대값은 데모 시드의 사실관계에서 유도한 값이며 **심사원·보안 전문가 검증 전**이다(PRD §14 오픈 이슈 4). 일치율은 '전문가 정답과의 일치'가 아니라 '우리가 데모 데이터에서 읽어낸 기대값과의 일치'다.
- 평가 대상은 가상 회사 문서 12개와 AWS 점검 10개로 만든 단일 프로젝트다. 실제 고객 데이터의 난이도를 대표하지 않는다.
- 이 실행은 `ANTHROPIC_API_KEY` 없이 결정적 Fake 프로바이더로 돌았다. 일치율·정밀도·재현율은 LLM 판정 품질이 아니라 파이프라인 배선의 재현성만 보여 준다. 근거 참조 유효율과 비용 지표는 그대로 유효하다.
