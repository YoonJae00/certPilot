# 백엔드 API 주소.
# 비워 두면(권장) 브라우저가 접속한 호스트 기준으로 자동 결정된다
# (예: http://192.168.x.x:3000 으로 열면 API 는 http://192.168.x.x:8000).
# 특정 주소로 고정할 때만 값을 채운다 — 값을 넣으면 자동 감지가 꺼진다.
# NEXT_PUBLIC_API_URL=http://localhost:8000
